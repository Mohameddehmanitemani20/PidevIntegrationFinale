{% extends 'back.html.twig' %}

{% block title %}Matchs{% endblock %}

{% block body %}
    <h1>Matchs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdMatch</th>
                <td>{{ match.idMatch }}</td>
            </tr>
            <tr>
                <th>Equipe1</th>
                <td>{{ match.equipe1 }}</td>
            </tr>
            <tr>
                <th>Equipe2</th>
                <td>{{ match.equipe2 }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ match.etat }}</td>
            </tr>
            <tr>
                <th>IdJourne</th>
                <td>{{ match.idJourne }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('app_journe_index') }}">back to list journes</a>
    <div>
        <a href="{{ path('app_matchs_index') }}">back to list Matchs</a>
    </div>


    <a href="{{ path('app_matchs_edit', {'idMatch': match.idMatch}) }}">edit</a>

    {{ include('matchs/_delete_form.html.twig') }}
{% endblock %}
