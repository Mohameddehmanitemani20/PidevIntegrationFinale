<form method="post" action="{{ path('app_journe_delete', {'idJourne': journe.idJourne}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ journe.idJourne) }}">
    <button class="btn">Delete</button>
</form>
