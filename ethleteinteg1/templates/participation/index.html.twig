{% extends 'back.html.twig' %}

{% block title %}Liste de participation{% endblock %}

{% block body %}
    <h1>Consultation des participations</h1>

    <table class="table">
        <thead>
            <tr>
                {# <th>IdParticipation</th> #}
                <th>DateParticipation</th>
                   <th>Participant</th>
                      <th>Formation</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
        {% for participation in participations %}
            <tr>
                {# <td>{{ participation.idParticipation }}</td> #}
                <td>{{ participation.dateParticipation ? participation.dateParticipation|date('Y-m-d') : '' }}</td>
                   <td>{{ participation.idParticipant.username }}</td>
                      <td>{{ participation.formation.nomFormation }}</td>
                <td>
                    {# <a href="{{ path('participation_show', {'idParticipation': participation.idParticipation}) }}">show #}
                                                {{ include('participation/_delete_form.html.twig') }}

                    
                    </a>
                    {# <a href="{{ path('participation_edit', {'idParticipation': participation.idParticipation}) }}">edit</a> #}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
