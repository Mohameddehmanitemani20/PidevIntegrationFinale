<form method="post" action="{{ path('app_raison_delete', {'idraison': raison.idraison}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ raison.idraison) }}">
    <button class="btn">Delete</button>
</form>
